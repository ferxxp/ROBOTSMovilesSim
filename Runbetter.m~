str1='maps/map';
str2='/map';
str3='.csv';
for a= 1:3
load(sprintf('%s%d%s%d%s',str1,a,str2,a,str3))
end

Robot=RobotGen(map1,[2,2,0],[0,10]);
updatePlotRobot(Robot);

[distancia, angulo] =  escanearAlrededores(Robot);
mostrarScan(Robot,distancia,angulo)

Robot.PosReal=[2,2,1];

[distancia, angulo] =  escanearAlrededores(Robot);
mostrarScan(Robot,distancia,angulo)
